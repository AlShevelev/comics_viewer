buildscript {
    apply from: "$rootDir/dependencies.gradle"

    repositories {
        jcenter()
        google()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:$build.gradle_plugin"
    }
}

allprojects {
    repositories {
        jcenter()
        google()
    }
}
